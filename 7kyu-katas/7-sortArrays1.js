// Just a simple sorting usage. Create a function that 
// returns the elements of the input-array / list 
// sorted in lexicographical order.

function sortme(names){
    let sorted = names.sort()

    return sorted
}